// You can use enums to represent a set of predefined values in your code
// enum is read only

enum ResourceType {
  book,
  author,
  film,
  director,
  person,
}

// By default enum assign with a number start with 0.
// But you can assign values too.

enum CustomResourceType {
  book = 10,
  author = "mac",
}
