// explcit types
let character: string;
let age: number;
let isLogged: boolean;

// arrays
let namesArray: string[];
let nameInit: string[] = [];

// union types
let mixed: (string | number)[] = [];
let uid: string | number;

// objects
let ninjaObj: object;
ninjaObj = {
  name: "random",
};

let objType: { name: any; age: any };
